# Python program to demonstrate exit()

for i in range(10):

    # If value of i becomes 5, the program is forced to exit
    if i == 6:

        # Print the exit message
        print(exit)
        exit()
    print(i)
